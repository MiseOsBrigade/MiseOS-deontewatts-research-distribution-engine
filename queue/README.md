# Distribution Queue

`current.json` is generated atomically with each catalog record. It identifies exactly one record and the files allowed into the next Zenodo synchronization run. Processed queue state remains auditable in Git history.
